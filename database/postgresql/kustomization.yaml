apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: myapp

resources:
  - configmap.yaml
  - service.yaml
  - statefulset.yaml

secretGenerator:
  - name: postgresql-secret
    type: Opaque
    literals:
      - USER_PASSWORD=changeme-user-password

generatorOptions:
  disableNameSuffixHash: true
